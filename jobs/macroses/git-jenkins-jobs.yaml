- scm:
    name: git-ui-tests
    scm:
    - git:
        url: git@github.com:LutsenkoAnna/OTUS_QA.Java.git
        credentials-id: jenkins
        branches:
        - FETCH_HEAD
        refspec: ${BRANCH}
- scm:
    name: git-api-tests
    scm:
    - git:
        url: git@github.com:LutsenkoAnna/OTUS_QA.Java.git
        credentials-id: jenkins
        branches:
        - FETCH_HEAD
        refspec: ${BRANCH}
- scm:
    name: git-appium-tests
    scm:
    - git:
        url: git@github.com:LutsenkoAnna/OTUS_QA.Java.git
        credentials-id: jenkins
        branches:
        - FETCH_HEAD
        refspec: ${BRANCH}